# Copia este archivo a ".env" y completa tus datos reales.
# NUNCA subas el archivo .env real a git.

PGHOST=localhost
PGPORT=5432
PGDATABASE=proveedores_colombia
PGUSER=postgres
PGPASSWORD=Erick1019

# Segundos de espera entre solicitudes a un mismo sitio (buenas prácticas / no saturar al proveedor)
SCRAPE_DELAY_SECONDS=2
